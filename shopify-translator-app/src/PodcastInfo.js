import React from "react";
import styled from "styled-components";

function PodcastInfo() {
  return <PodcastInfoWrapper>hello</PodcastInfoWrapper>;
}

const PodcastInfoWrapper = styled.div``;

export default PodcastInfo;
